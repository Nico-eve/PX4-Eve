menuconfig MODULES_EVE_NAVIGATOR
	bool "eve_navigator"
	default n

menuconfig USER_NAVIGATOR
	bool "eve_navigator running as userspace module"
	default y
	depends on BOARD_PROTECTED && MODULES_NAVIGATOR
	---help---
		Put navigator in userspace memory
